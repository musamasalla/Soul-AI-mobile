import SwiftUI

extension Color {
    static let brandMint = Color("AppColors")
    
    // Dynamic background color based on color scheme
    static let brandBackground = Color("BackgroundColor")
    
    // Dynamic text colors
    static let primaryText = Color("PrimaryText")
    static let secondaryText = Color("SecondaryText")
    
    // Dynamic UI element colors
    static let cardBackground = Color("CardBackground")
    static let inputBackground = Color("InputBackground")
} 